<h2>
    <a href="{{ path('job_show') }}" class="title">
        {{ job.title }}
    </a>
</h2>

<div class="details">
    <span class="filter">
        <a href="#">{{ job.city }}, {{ job.countryname }}</a> /
        <a href="#">{{ job.contracttypetext }}</a>
    </span>
</div>

<span class="company">@&nbsp; {{ job.company }}</span>
